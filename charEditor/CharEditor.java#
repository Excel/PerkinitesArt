
import com.google.gson.*;

public class CharEditor {
	public static void main(String[] args) throws Exception {
		File cwd = new File(".");
		String path = cwd.getCanonicalPath() + "\\assets\\data\\characters\\";
		
		JsonReader reader = new JsonReader(new FileReader(path + "characters.json"));
		
		reader.beginObject();
		reader.setLenient(true);
		
		String name= reaer.nextName();
		String 
		
		showGUI();
	}
	
	public static function showGUI() {
		
	}
}
